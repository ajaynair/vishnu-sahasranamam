
export interface LanguageOption {
  id: string;
  name: string; // e.g., "English", "Hindi"
  displayName: string; // e.g., "English", "हिन्दी (Hindi)"
  contentText: string; // The content text with \n for newlines
}